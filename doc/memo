AWSへのECS,ECR,Fargateを使ったデプロイはできた。(supabase)

まだできていないこと
RDSをデータベースとした設定

##試すこと
・最初はパブリックで作成して、色々設定着ける前にpushができるか確認その後privateにしたり諸々設定する



原因はprivatesubnetにRDSが存在していて、かつルートテーブルにインターネットゲートウェイがないのが原因っぽかったです